{
  "name": "md-links",
  "version": "0.1.0",
 
  "scripts": {
    
    "eslint": "eslint --ext .js src/ test/",
    "pretest": "npm run eslint && npm run htmlhint",
    "test": "jest --verbose --coverage"
  },
  "dependencies": {
    "babel-jest": "27.0.1",
    "eslint": "^7.12.1",
    "jest": "^27.0.1"
  }
 
}