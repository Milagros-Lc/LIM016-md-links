
{
  "name": "md-links",
  "version": "1.0.0",
 
  "license": "MIT",
  "scripts": {
    
    "eslint": "eslint --ext .js test/",
    "pretest": "npm run eslint && npm run ",
    "test": "jest --verbose --coverage",
    "open-coverage-report": "opener ./coverage/lcov-report",
    "start": "serve ",
    "deploy": "gh-pages -d "
  },
  "dependencies": {
    "@babel/core": "^7.6.2",
    "@babel/plugin-transform-modules-commonjs": "^7.6.0",
    "babel-jest": "27.0.1",
    "eslint": "^7.12.1",
    "fetch": "^1.1.0",
    "gh-pages": "^3.1.0",
    "htmlhint": "^0.15.1",
    "jest": "^27.0.1",
    "node-fetch": "^3.1.0",
    "opener": "^1.5.1",
    "serve": "^12.0.0"
  },
 
  "devDependencies": {
    "jest-fetch-mock": "^3.0.3"
  }
}
